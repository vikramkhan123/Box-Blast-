<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
<uses-permission android:name="android.permission.VIBRATE" />
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        
        <!-- Aapki ORIGINAL Google AdMob App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4346513942475662~8153382794"/>

        <activity android:name=".LevelSelectionActivity" />
        <!-- Main Menu Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ClassicGameActivity"
            android:theme="@style/Theme.MyApplication"
            android:screenOrientation="portrait" />
        
        <activity
            android:name=".AdventureGameActivity"
            android:theme="@style/Theme.MyApplication"
            android:screenOrientation="portrait" />
            
        <activity
            android:name=".TetrisGameActivity"
            android:theme="@style/Theme.MyApplication"
            android:screenOrientation="portrait" />
    </application>
</manifest>
